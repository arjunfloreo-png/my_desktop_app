

// ── Reward badge model ───────────────────────────────────────
import 'package:flutter/material.dart';

class RewardBadge {
  final String label;
  final String emoji;
  final Color bgColor;
  final String name;
  RewardBadge({
    required this.label,
    required this.emoji,
    required this.bgColor,
    required this.name,
  });
}